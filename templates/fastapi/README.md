# DevForge FastAPI Template

Production-grade template for building FastAPI services in DevForge.

## Structure
- `app/`: Source code.
- `tests/`: Tests using pytest.

## Execution
Run from the root of DevForge:
```bash
docker compose restart fastapi
```
