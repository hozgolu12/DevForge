# DevForge AI Platform Template

Production-grade template for building LangChain, LangGraph, LlamaIndex, and CrewAI projects in DevForge.

## Structure
- `app/`: Source code.
- `tests/`: Tests using pytest.

## Execution
Run from the root of DevForge:
```bash
docker compose restart ai
```
