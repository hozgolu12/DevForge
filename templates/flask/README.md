# DevForge Flask Template

Production-grade template for building Flask services in DevForge.

## Structure
- `app/`: Source code.
- `tests/`: Tests using pytest.

## Execution
Run from the root of DevForge:
```bash
docker compose restart flask
```
