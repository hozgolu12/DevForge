# DevForge Django Template

Production-grade template for building Django applications in DevForge.

## Structure
- `config/`: Project configuration and settings.
- `core/`: Core application logic (views, tests).

## Execution
Run from the root of DevForge:
```bash
docker compose restart django
```
